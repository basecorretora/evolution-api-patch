diff --git a/src/api/dto/sendMessage.dto.ts b/src/api/dto/sendMessage.dto.ts
index ba9ecf5..2d4425c 100644
--- a/src/api/dto/sendMessage.dto.ts
+++ b/src/api/dto/sendMessage.dto.ts
@@ -14,6 +14,8 @@ export class Options {
   mentionsEveryOne?: boolean;
   mentioned?: string[];
   webhookUrl?: string;
+  /** [PATCH BASE CORRETORA] marca a mensagem como encaminhada (etiqueta no aparelho) */
+  forwarded?: boolean;
 }
 
 export class MediaMessage {
@@ -45,6 +47,8 @@ export class Metadata {
   mentioned?: string[];
   encoding?: boolean;
   notConvertSticker?: boolean;
+  /** [PATCH BASE CORRETORA] marca a mensagem como encaminhada (etiqueta no aparelho) */
+  forwarded?: boolean;
 }
 
 export class SendTextDto extends Metadata {
diff --git a/src/api/integrations/channel/whatsapp/whatsapp.baileys.service.ts b/src/api/integrations/channel/whatsapp/whatsapp.baileys.service.ts
index 60e857f..2f9029b 100644
--- a/src/api/integrations/channel/whatsapp/whatsapp.baileys.service.ts
+++ b/src/api/integrations/channel/whatsapp/whatsapp.baileys.service.ts
@@ -2195,6 +2195,24 @@ export class BaileysStartupService extends ChannelStartupService {
     }
 
     if (message['conversation']) {
+      // [PATCH BASE CORRETORA] encaminhada: texto puro perde o contextInfo no
+      // Baileys (vira `conversation`). O forward nativo com force gera
+      // extendedTextMessage com isForwarded/forwardingScore no pacote.
+      if (message['contextInfo']?.isForwarded) {
+        return await this.client.sendMessage(
+          sender,
+          {
+            forward: {
+              key: { remoteJid: this.instance.wuid, fromMe: true },
+              message: { conversation: message['conversation'] },
+            },
+            force: true,
+            mentions,
+            contextInfo: message['contextInfo'],
+          } as unknown as AnyMessageContent,
+          option as unknown as MiscMessageGenerationOptions,
+        );
+      }
       return await this.client.sendMessage(
         sender,
         {
@@ -2212,6 +2230,8 @@ export class BaileysStartupService extends ChannelStartupService {
         sender,
         {
           forward: { key: { remoteJid: this.instance.wuid, fromMe: true }, message },
+          // [PATCH BASE CORRETORA] mídia já sai por forward; force = etiqueta
+          force: !!message['contextInfo']?.isForwarded,
           mentions,
           contextInfo: message['contextInfo'],
         },
@@ -2392,6 +2412,8 @@ export class BaileysStartupService extends ChannelStartupService {
           quoted,
           null,
           group?.ephemeralDuration,
+          // [PATCH BASE CORRETORA] etiqueta "Encaminhada" no aparelho do destinatário
+          options?.forwarded ? { isForwarded: true, forwardingScore: 1 } : undefined,
           // group?.participants,
         );
       } else {
@@ -2406,6 +2428,10 @@ export class BaileysStartupService extends ChannelStartupService {
           },
           disappearingMode: { initiator: 0 },
         };
+        // [PATCH BASE CORRETORA] etiqueta "Encaminhada" no aparelho do destinatário
+        if (options?.forwarded) {
+          contextInfo = { ...contextInfo, isForwarded: true, forwardingScore: 1 };
+        }
         messageSent = await this.sendMessage(
           sender,
           message,
@@ -2643,6 +2669,7 @@ export class BaileysStartupService extends ChannelStartupService {
         linkPreview: data?.linkPreview,
         mentionsEveryOne: data?.mentionsEveryOne,
         mentioned: data?.mentioned,
+        forwarded: data?.forwarded,
       },
       isIntegration,
     );
@@ -2994,6 +3021,7 @@ export class BaileysStartupService extends ChannelStartupService {
         quoted: data?.quoted,
         mentionsEveryOne: data?.mentionsEveryOne,
         mentioned: data?.mentioned,
+        forwarded: data?.forwarded,
       },
       isIntegration,
     );
@@ -3224,7 +3252,7 @@ export class BaileysStartupService extends ChannelStartupService {
         const result = this.sendMessageWithTyping<AnyMessageContent>(
           data.number,
           { audio: convert, ptt: true, mimetype: 'audio/ogg; codecs=opus' },
-          { presence: 'recording', delay: data?.delay },
+          { presence: 'recording', delay: data?.delay, forwarded: data?.forwarded },
           isIntegration,
         );
 
@@ -3241,7 +3269,7 @@ export class BaileysStartupService extends ChannelStartupService {
         ptt: true,
         mimetype: 'audio/ogg; codecs=opus',
       },
-      { presence: 'recording', delay: data?.delay },
+      { presence: 'recording', delay: data?.delay, forwarded: data?.forwarded },
       isIntegration,
     );
   }
